name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  sanity:
    name: Sanity
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: No-op check
        run: echo "ok"
